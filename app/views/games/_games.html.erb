<table id="teams" class="table table-striped">
  <% @games.each do |game| %>
    <tr class="update-bars" id="<%=game.id%>">
      <td>
        <%= image_tag(game.away_team.small_logo_path, :alt => '')%> 
         &nbsp;@&nbsp;
        <%= image_tag(game.home_team.small_logo_path, :alt => '')%> 
      </td>
      <td>
        <%= game.game_at.time.strftime('%A %l:%M %p') %> 
      </td>
    </tr>
  </a>
  <% end %>
</table>
